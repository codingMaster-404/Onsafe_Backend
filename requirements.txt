fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# Firebase / Firestore
firebase-admin==6.5.0
google-cloud-firestore[async]==2.19.0

# Redis (async)
redis>=5.0.0

# HTTP client (Python → Kotlin internal API 호출)
httpx>=0.27.0

# JWT
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0

# AI (fall detection)
# xgboost/numpy/scipy/scikit-learn은 pkl/xgb_model.pkl, pkl/scaler.pkl 학습 환경과
# 버전이 어긋나면 로드 실패 또는 예측값이 조용히 달라질 수 있어 학습 환경 값으로 고정한다.
xgboost==2.1.4
numpy==2.1.3
pandas>=2.0.0
joblib>=1.3.0
scipy==1.14.1
scikit-learn==1.6.1
