# 🛡️ OnSafe API 명세서 v4.0

> 낙상 감지 앱 백엔드 REST API 사양서  
> **버전:** v4.0 | **기술스택:** FastAPI(Python AI) + Spring WebFlux(Kotlin) + Firebase + Redis / Kotlin(클라이언트)  
> **v3.x 대비 핵심 변경**: Python AI 서버 인터페이스 HTTP+JPEG → **WebSocket+landmark** 전환

---

## 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.0 | 2025-04-15 | 팀 전체 | 초안 작성 (3차 회의록 기반) |
| v2.0 | 2025-04-29 | 팀 전체 | DB 명세서 반영 — 필드명 교정 (`risk_score`→`score`, `is_fall`→`fall`, `event_time`→`timestamp`, `guardian_name`→`name`, `email`→`mail`), devices API 추가, 낙상 확인 처리 엔드포인트 추가, 23개 엔드포인트 확정 |
| v3.0 | 2026-05-13 | 팀 전체 | 아래 상세 참고 |
| v3.1 | 2026-05-25 | 팀 전체 | 설정 항목 정리 — `fall_sensitivity` 제거(AI 내부 고정값), `retention_days` 서버 고정 30일(PUT 엔드포인트 제거), 알림 설정에 `sound_enabled`·`vibration_enabled` 반영 |
| v3.2 | 2026-05-27 | 팀 전체 | 예외처리 강화 — AWS SES `SdkClientException` 처리 추가, FCM 실패 아키텍처 개선(Option A), JWT 토큰 에러 JSON 응답 보장, 405 핸들러 추가, `FCM_SEND_FAILED` ErrorCode 추가 |
| v4.0 | 2026-05-29 | 팀 전체 | **AI 서버 인터페이스 전면 전환** — `POST /api/camera/stream` (HTTP+JPEG) → `WS /ws/stream` (WebSocket+landmark). Android on-device MediaPipe → landmark JSON 송출 방식 채택. XGBoost 모델 교체(Decision Tree → XGBoost), 47개 피처·30프레임 윈도우. `GET /health` 엔드포인트 추가. `StreamResponse`에 `level` 필드 추가 |
| v4.1 | 2026-06-03 | 팀 전체 | `POST /api/users/{userId}/verify-password` 신규 (개인정보 수정 전 본인 확인). `GET /api/fall-logs/{userId}/{logId}/download` 삭제 (`/thumbnail`로 대체). `POST /api/notification/send` 삭제 (NotificationController 제거). Kotlin `GET /api/camera/score` 응답에 `updated_at` 추가. score 임계값 `>=` 경계값 포함으로 통일. engine.py에 `_classify_level()` 내재화, service.py 중복 제거 |
| v4.2 | 2026-08-05 | 팀 전체 | **PR #19~#24 반영, 스펙-코드 정합화**. ① **score 임계값 경계값 재수정** — v4.1의 `>=`(51/76) 통일 이후 실제 코드는 `f62a153`/`ea13763`(2026-06)에서 다시 `>`(50/75) strict 경계로 정정됐으나 본 문서가 그 반영을 놓치고 있었음. 정상(0~50)/주의(50 초과~75)/위험(75 초과) — `RiskLevel.kt`·`app/ai/engine.py` 직접 확인. ② **썸네일(JPEG)→동영상(MP4) 전환**(PR #19) — `hasThumbnail`→`hasVideo`, `GET /thumbnail`→`GET /video`, 업로드 3종 API(`POST upload-url`→`PUT` GCS 직접 업로드→`PATCH video-complete`) 신규, 위험 등급 전용 정책(`VIDEO_NOT_ALLOWED`), 알림 정책 개선(6h danger 쿨다운·2.5초 스무딩·15분 sticky floor), 에스컬레이션/정합성 보정 스케줄러 2종 신규. ③ **개인정보 컴플라이언스**(PR #20) — 로그인 이력 저장, 회원탈퇴 시 cascade 삭제, `video_url` AES-256-GCM 암호화. ④ **마케팅 수신 동의**(PR #21) — `GET/PUT /api/settings/marketing/{userId}` 신규. ⑤ **`log_id` 필드**(PR #22) — WS `result` 메시지에 추가, 미사용 Kotlin `/api/devices` 중복 제거. ⑥ **죽은 카메라 라이브 릴레이 전체 제거**(PR #23) — `/ws/camera`, 카메라 세션 3종, `PUT /api/camera/url`(Kotlin), `POST /internal/frame` 삭제. 총 REST **38개**(Kotlin 33 + Python 5) + WS 1개 |

### v3.0 상세 변경 내역 (2026-05-13)

#### 🏗️ 아키텍처
- 이중 서버 구조 명시 — Python AI 서버(FastAPI)와 Kotlin 서버(Spring WebFlux) 역할 분리
- 응답 형식 분리 — Python: flat JSON / Kotlin: `{ success, message, data }` 래퍼
- 서버간 통신 흐름 추가 — `POST /internal/frame/{userId}` → Redis pub/sub → WebSocket

#### 🔢 수치 오류 수정
- `score` 범위: `0.0~1.0` → **`0~100`** (코드 실측값 반영: `predict_proba * 100`)
- 위험 레벨 임계값: `0.4/0.7` → **정상(0~50) / 주의(51~75) / 위험(76~100)** 으로 수정
- 낙상 로그 저장 조건: `fall == true` → **`score ≥ 76 OR fall == true`**

#### 🔐 인증·보안
- 로그인 응답에 **`access_token`, `refresh_token`, `token_type`** 추가 (v2.0: 누락)
- 로그인 요청에 **`device_id`** 필드 추가 (v2.0: 누락), 응답에도 echo 반환
- 로그아웃: v2.0의 "토큰 블랙리스트 등록" 오기재 수정 — 실제 컨트롤러는 단순 ok 반환
- **`POST /api/auth/refresh`** 신규 — `Refresh-Token` 헤더로 토큰 재발급

#### 📋 Auth 엔드포인트 변경
- **`POST /api/auth/check-id`** 신규 — 아이디 중복확인
- **`POST /api/auth/send-email-code`** 신규 — 회원가입 이메일 인증코드 발송 (TTL 3분)
- **`POST /api/auth/verify-email-code`** 신규 — 회원가입 이메일 인증코드 확인
- 비밀번호 재설정 단일 엔드포인트 → **3단계 분리**
  - `POST /api/auth/send-reset-code` (코드 발송, TTL 3분)
  - `POST /api/auth/verify-reset-code` (코드 검증)
  - `POST /api/auth/reset-password` (비밀번호 변경, 서버 측 단계 검증 없음)
- 아이디 찾기 응답: userId 뒷자리 `*` 마스킹 처리 추가 (`user001` → `use****`)
- 회원가입 요청: `address`, `address_detail` 선택 필드 추가

#### 📷 카메라·스트리밍
- **`PUT /api/camera/url/{device_id}`** 신규 — Kotlin 서버에서 카메라 URL 업데이트
- **`PUT /api/camera/session/{device_id}/start`** 신규 — 세션 시작 (Redis CONNECTING)
- **`PUT /api/camera/session/{device_id}/stop`** 신규 — 세션 종료 (Redis 삭제 + STOP 신호)
- **`GET /api/camera/session/{user_id}/status`** 신규 — 세션 상태 폴링
- **`WS /ws/camera/{user_id}`** 신규 — JPEG 프레임 실시간 수신 (`?token=` 인증)
- `PATCH /api/fall-logs/{userId}/{logId}/confirm` 담당 서버 수정: Python → **Kotlin** (경로도 수정)

#### 🗄️ DB 스키마
- `users` 컬렉션: `wardName` 필드 제거
- `realtime_data` 컬렉션 신규 — AI 추론 시 관절 피처 51개 + score 자동 저장, 사용자별 최신 2,000개 유지
- `fall_logs.score` 범위 표기 수정: `0.0~1.0` → `0~100`

#### 🔑 Redis 키
- Kotlin 관리 키와 Python 관리 키로 분리하여 명시
- 신규 키 추가: `email_verify:{mail}`, `reset_code:{userId}`, `camera:session:*`, `camera:frames:{userId}`
- v2.0에서 누락된 Python 내부 키 추가: `frame_count:{device_id}`, `frame:{device_id}`, `score:{user_id}`

#### 🔧 내부 API
- **`POST /internal/frame/{userId}`** 신규 — Python AI 서버 → Kotlin 서버 프레임 전달 (JWT 없음)
- **`POST /internal/realtime`** — Python 추론 후 Kotlin 호출 → `realtime_data/{userId}` 저장 (`GET /api/camera/score` 정상화)
- **`POST /internal/fall-log`** — 낙상 감지 시 Python이 Kotlin 호출 → `fall_logs` 저장 + FCM 발송 중앙화

#### 🐛 실측 테스트 버그 수정 (2026-05-13)
- **내부 API JSON 필드명 오류** — `/internal/realtime`, `/internal/fall-log` 요청 시 camelCase(`userId`, `deviceId` 등)로 전송하면 Kotlin SNAKE_CASE 정책에 의해 역직렬화 실패 → **snake_case(`user_id`, `device_id`, `log_id`, `is_confirmed`)로 수정**
- **`PUT /api/camera/url` 요청 필드명** — `cameraUrl` → **`camera_url`** (동일 SNAKE_CASE 정책)
- **`POST /api/devices/{userId}` upsert** — 다른 사용자 소유 기기 재등록 시 409 → 소유권 이전 후 **200 updated** 반환으로 변경

#### 🐛 실측 테스트 버그 수정 (2026-05-19)
- **`POST /internal/fall-log` FCM 오류 시 500** — `NotificationService`에서 Firebase `sendAsync()` 예외를 catch하지 않아, FCM 토큰이 무효/만료된 경우 낙상 로그 저장 후 500 반환. **Option A** 구조로 수정 — `NotificationService`는 FCM 실패 시 `BusinessException(FCM_SEND_FAILED)` 던짐; `InternalService`는 `sendNotificationSafe()` wrapper로 예외를 흡수해 DB 저장 독립 보장. `POST /api/notification/send` 직접 호출 시에는 FCM 실패 → `500 FCM_SEND_FAILED` 반환
- **Python `GET /api/devices/{user_id}` 405** — `app/domain/devices/router.py`에 GET 라우트 미등록. `get_devices()` 함수 및 라우트 추가로 수정
- **`realtime_data` Firestore 복합 인덱스** — `user_id ASC + timestamp DESC` 인덱스 생성 필요 (미생성 시 최신 2,000개 유지 쿼리 실패)
- **`GET /api/devices/{userId}` 빈 목록** — `.where().get()` → `.where().stream()` 교체로 Firestore async 쿼리 누락 수정

#### 📊 엔드포인트 수
- v2.0: 23개 (알림 엔드포인트 요약 테이블 누락 포함) → v3.0: **37개 REST + 1 WebSocket**

#### 📄 문서 구조
- Python AI 서버 구현 상세(처리 흐름, 모델 정보, realtime_data 필드)를 **부록 A**로 분리

### v3.2 상세 변경 내역 (2026-05-27)

#### 🔐 JWT 토큰 에러 응답
- **`JwtAuthenticationFilter` JSON 응답 보장** — 만료(`EXPIRED_TOKEN`) 및 무효(`INVALID_TOKEN`) 토큰 시 Spring Security 기본 403 대신 JWT 필터에서 직접 `401` + `{"success":false,"message":"...","data":null}` 반환
- **`JwtProvider.getValidationError()`** — `ExpiredJwtException` → `EXPIRED_TOKEN`, 그 외 예외 → `INVALID_TOKEN` 구분. `validate()`는 해당 메서드 위임

#### 📧 이메일 발송 예외처리 강화
- **`SdkClientException` 처리 추가** — 네트워크 단절·타임아웃·AWS 장애 시 발생하는 `SdkClientException`을 별도 catch, `log.error` 후 `MAIL_SEND_FAILED` 반환 (기존: `SesException`만 처리해 500 raw 노출)
- `SesException.awsErrorDetails()` null-safe 접근(`?.`) 추가 (NPE 방지)

#### 🔔 FCM 실패 아키텍처 개선 (Option A)
- **`NotificationService`** — FCM 실패 시 `200 {"status":"error"}` 반환 대신 `BusinessException(FCM_SEND_FAILED)` throw. `POST /api/notification/send` 직접 호출 시 `500` 반환
- **`InternalService.sendNotificationSafe()`** — `runCatching { }` wrapper로 FCM 예외를 흡수; DB 저장과 FCM 발송을 독립 보장. AI 서버 재시도 시 중복 `fall_log` 방지

#### 🆘 에러코드 추가
- **`FCM_SEND_FAILED`** — `500 INTERNAL_SERVER_ERROR`, 메시지: "알림 전송에 실패했습니다."

#### 🔧 예외 핸들러 추가
- **`GlobalExceptionHandler`** — `MethodNotAllowedException` 핸들러 추가 → `405 METHOD_NOT_ALLOWED`, 메시지: "지원하지 않는 HTTP 메서드입니다."

#### 🗑️ 엔드포인트 제거 (v3.1 반영)
- **`PUT /api/settings/retention/{user_id}`** — 엔드포인트 요약 테이블에서 제거 (v3.1에서 서버 고정 30일로 변경, PUT 미지원)

---

## ⚠️ v2.0 → v3.0 주요 변경 사항

| 항목 | v2.0 | v3.0 | 근거 |
|------|------|------|------|
| 서버 구조 | 단일 서버로 기술 | Python AI 서버 + Kotlin 서버 분리 명시 | `app/main.py` 라우터 구성 |
| score 범위 | `0.0 ~ 1.0` | **`0 ~ 100`** | `engine.py: predict_proba * 100` |
| 위험 레벨 임계값 | 정상(0~0.4) / 주의(0.4~0.7) / 위험(0.7~1.0) | **정상(0~50) / 주의(51~75) / 위험(76~100)** | `service.py: _score_level()`, `RiskLevel.kt` |
| 낙상 로그 저장 조건 | `fall == True` | **`score ≥ 76 OR fall == True`** | `service.py: if score >= 76 or fall:` |
| 로그인 응답 | `status, user_id, device_id, name` | **JWT accessToken + refreshToken 포함** | `security.py: generate_access_token/refresh_token` |
| 로그아웃 | Body로 user_id | **`Authorization: Bearer {token}` 헤더 → 토큰 블랙리스트** | CHANGELOG 2026-05-11 |
| 비밀번호 재설정 | 단일 단계 | **2단계: 코드 발송 → 코드 검증 → 비밀번호 변경** | CHANGELOG 2026-05-11 |
| users 컬렉션 | wardName 포함 | **wardName 필드 제거** | CHANGELOG `b3495e7` |
| Firestore 컬렉션 | 4개 | **5개 (realtime_data 추가)** | `service.py: _REALTIME = "realtime_data"` |
| PATCH confirm 경로·담당 | Python AI 서버, `/api/camera/fall-logs/{log_id}/confirm` | **Kotlin 서버, `/api/fall-logs/{userId}/{logId}/confirm`** | CHANGELOG `a1da93a` FallLogController |
| 신규 엔드포인트 | 없음 | **check-id, 카메라 URL 수정, 세션 3종, WebSocket, 내부 API** | CHANGELOG / docs |
| 응답 형식 | 전부 flat JSON | **Python: flat / Kotlin: `{ success, data }` 래퍼** | `common/response.py` 주석 |
| deviceId 생성 방식 | `{userId}_device` | **`ANDROID_ID` (기기 고유 ID)** | CHANGELOG `b3495e7` Android 변경 |

---

## 🏗️ 서버 아키텍처

```
Android 클라이언트
    │
    ├─── REST ──────────────────→ Kotlin Spring Backend (포트 8080)
    │                             인증 · 유저 · 설정 · 낙상 로그
    │
    └─── REST ──────────────────→ Python AI Server (FastAPI)
                                  카메라 프레임 전송 + AI 추론
                                  기기 등록 · 조회
```

---

## 🗄️ Firestore 컬렉션 구조

### 📁 users

| 필드 | 타입 | 설명 |
|------|------|------|
| `user_id` | string | 문서 ID, PK |
| `name` | string | 보호자 이름 (~~wardName~~ · ~~guardian_name~~ 제거됨) |
| `password` | string | bcrypt 해시 비밀번호 |
| `phone` | string | 전화번호 |
| `mail` | string | 이메일 (~~email~~) |
| `address` | string | 주소 (선택) |
| `address_detail` | string | 상세 주소 (선택) |
| `fcm_token` | string | FCM 푸시 토큰 |
| `created_at` | timestamp | 가입 시각 |

### 📁 devices

| 필드 | 타입 | 설명 |
|------|------|------|
| `device_id` | string | 문서 ID, PK (`ANDROID_ID` 사용) |
| `status` | string | `active` / `inactive` / `offline` |
| `user_id` | string | 소유 사용자 ID (FK) |
| `device_name` | string | 기기 이름 (`Build.MODEL`) |
| `last_seen` | timestamp | 마지막 접속 시각 |

### 📁 fall_logs

| 필드 | 타입 | 설명 |
|------|------|------|
| `log_id` | string | 문서 ID, PK (UUID auto) |
| `device_id` | string | 기기 ID (FK, 필수) |
| `user_id` | string | 사용자 ID (FK) |
| `score` | float | 낙상 위험 점수 **0~100** (~~risk_score~~, ~~0.0~1.0~~) |
| `fall` | boolean | 낙상 여부 (~~is_fall~~) |
| `is_confirmed` | boolean | 보호자 확인 여부 (기본 false) |
| `video_url` | string? | 낙상 동영상 GCS 경로, AES-256-GCM 암호화 저장 (v4.2, ~~image_url~~ 대체) |
| `last_reminder_at` | timestamp? | 미확인 위험 이벤트 에스컬레이션 리마인더 마지막 발송 시각 (v4.2 신규) |
| `timestamp` | timestamp | 저장 시각 (UTC) |

> ⚠️ v4.2: 저장 배관이 썸네일(JPEG)→동영상(MP4)로 전환되며 `image_url`이 `video_url`로 대체됐습니다. `video_url`은 위험(75 초과) 등급 로그에만 채워지며(`VIDEO_NOT_ALLOWED` 정책), Firestore 직접 읽기/쓰기 시 반드시 `EncryptionService`로 암·복호화해야 합니다 — 우회 시 평문 저장 버그가 재발합니다 (PR #20에서 한 차례 발견·수정됨).

### 📁 login_history ✨ 신규 (v4.2)

> 로그인 성공·실패 시도를 감사 로그로 기록. 클라이언트 직접 조회 API 없음 — 침입 시도 감지·사후 조사용.

| 필드 | 타입 | 설명 |
|------|------|------|
| `history_id` | string | 문서 ID, PK (auto) |
| `user_id` | string | 사용자 ID (FK) |
| `ip_address` | string | 요청 IP (`X-Forwarded-For` 우선, 없으면 remote address) |
| `user_agent` | string | 요청 User-Agent |
| `success` | boolean | 로그인 성공 여부 |
| `fail_reason` | string? | 실패 사유 (실패 시) |
| `timestamp` | timestamp | 시도 시각 |

> 회원 탈퇴 시 `fall_logs`·`settings`와 함께 cascade 삭제됩니다 (v4.2, 개인정보 즉시 파기 정책).

### 📁 settings

| 필드 | 타입 | 설명 |
|------|------|------|
| `user_id` | string | 문서 ID (FK) |
| `notification_enabled` | boolean | 알림 활성화 여부 |
| `sound_enabled` | boolean | 소리 알림 여부 |
| `vibration_enabled` | boolean | 진동 알림 여부 |
| `marketing_consent` | boolean | 마케팅 수신 동의 여부 (기본 false, v4.2 신규) |
| `marketing_consented_at` | timestamp? | 마케팅 수신 동의 시각 (v4.2 신규) |

> ⚠️ `fall_sensitivity`, `retention_days` 필드 제거됨 — 낙상 감도는 AI 서버 내부 고정값, 보관 기간은 서버 고정 30일 적용

### 📁 realtime_data ✨ 신규

> Python AI 서버가 추론 시 자동 저장하는 내부 컬렉션. 클라이언트 직접 조회 API 없음.  
> 상세 필드 정의는 [부록 A. Python AI 서버 구현 상세](#부록-a-python-ai-서버-구현-상세) 참고.

> ⚠️ **Firestore 복합 인덱스 필요** — 사용자별 최신 2,000개 유지 쿼리 동작을 위해 아래 인덱스를 Firebase 콘솔에서 생성해야 합니다.
>
> | 컬렉션 | 필드 | 정렬 |
> |--------|------|------|
> | `realtime_data` | `user_id` | Ascending |
> | `realtime_data` | `timestamp` | Descending |

---

## 🔑 인증 방식

모든 보호 엔드포인트는 `Authorization: Bearer {accessToken}` 헤더가 필요합니다.

| 항목 | 값 |
|------|-----|
| 알고리즘 | HS256 |
| Access Token 만료 | 3,600초 (1시간) |
| Refresh Token 만료 | 604,800초 (7일) |
| 로그아웃 시 | Redis `bl:{token}` 블랙리스트 등록 (남은 TTL 적용) |

### Redis 키 구조

**Kotlin 서버 관리 키**

| 키 | 값 | TTL |
|----|-----|-----|
| `bl:{token}` | 로그아웃 블랙리스트 | 토큰 남은 만료 시간 |
| `email_verify:{mail}` | 회원가입 이메일 인증 코드 (6자리) | 3분 |
| `reset_code:{userId}` | 비밀번호 재설정 인증 코드 (6자리) | 3분 |
| `reset_verified:{userId}` | 2단계(코드 검증) 완료 플래그 — `resetPassword` 호출 전 필수 | 10분 |

**Python AI 서버 관리 키** (내부 전용 — 상세는 [부록 A](#부록-a-python-ai-서버-구현-상세) 참고)

| 키 | 값 | TTL |
|----|-----|-----|
| `score:{user_id}` | `{"score": float, "level": string}` JSON | 30초 |
| `frame_count:{device_id}` | 프레임 카운터 (int) | 10초 |
| `frame:{device_id}` | JPEG bytes (최신 프레임) | 5초 |
| `caution_cd:{user_id}` | 주의(50 초과~75) 이벤트 쿨다운 플래그 — 5분 내 중복 알림 방지 | 5분 |
| `danger_cd:{user_id}` | 위험(75 초과)·`fall` 이벤트 쿨다운 플래그 — 6시간 내 중복 알림 방지 | 6시간 |
| `score_floor:{user_id}` | 위험 진입 시 점수 하락 방지용 최고값 유지 (sticky, 상승은 허용) | 15분 |

---

## 🔐 1. 인증 (Auth) — Kotlin 서버

### `POST /api/auth/login` — 로그인

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `user_id` | string | ✅ | 사용자 ID |
| `password` | string | ✅ | 비밀번호 |
| `device_id` | string | ✅ | 기기 ID (`ANDROID_ID`) |

**Response 200**
```json
{
  "success": true,
  "data": {
    "user_id": "user001",
    "device_id": "a1b2c3d4e5f6...",
    "name": "홍길동",
    "access_token": "eyJhbGc...",
    "refresh_token": "eyJhbGc...",
    "token_type": "Bearer"
  }
}
```

> 로그인 성공 후 Android 클라이언트는 `POST /api/devices/{userId}` 를 호출해 기기를 등록합니다 (409 이미 등록은 정상 처리).

**Response 401**
```json
{ "success": false, "message": "아이디 또는 비밀번호가 올바르지 않습니다" }
```

---

### `POST /api/auth/logout` — 로그아웃

> `Authorization: Bearer {token}` 헤더로 access token 수신 → Redis `bl:{token}` 블랙리스트 등록 (남은 만료 TTL 적용).  
> 이후 해당 토큰으로의 모든 요청은 401 차단됩니다.

**Response 200**
```json
{ "success": true, "message": "로그아웃 완료", "data": null }
```

---

### `POST /api/auth/register` — 회원가입

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `user_id` | string | ✅ | 사용자 ID |
| `password` | string | ✅ | 비밀번호 (4자 이상) |
| `name` | string | ✅ | 보호자 이름 |
| `mail` | string | ✅ | 이메일 |
| `phone` | string | ✅ | 전화번호 (형식: `010-0000-0000`) |
| `address` | string | ❌ | 주소 |
| `address_detail` | string | ❌ | 상세 주소 |

**Response 201**
```json
{ "success": true, "message": "회원가입이 완료되었습니다.", "data": null }
```

**Response 409**
```json
{ "success": false, "message": "이미 존재하는 아이디입니다" }
```

---

### `POST /api/auth/check-id` — 아이디 중복확인 ✨ 신규

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `user_id` | string | ✅ | 확인할 사용자 ID |

**Response 200** (사용 가능)
```json
{ "success": true, "message": "사용 가능한 아이디입니다" }
```

**Response 409** (중복)
```json
{ "success": false, "message": "이미 존재하는 아이디입니다" }
```

---

### `POST /api/auth/find-id` — 아이디 찾기

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `name` | string | ✅ | 보호자 이름 |
| `mail` | string | ✅ | 이메일 |

**Response 200**
```json
{
  "success": true,
  "message": "요청이 성공했습니다.",
  "data": { "user_id": "use****" }
}
```

> `user_id`는 앞 3자리만 표시하고 나머지는 `*`로 마스킹합니다. (예: `user001` → `use****`)

---

### 비밀번호 재설정 — 3단계 플로우

```
POST /api/auth/send-reset-code   → 인증코드 이메일 발송
          ↓
POST /api/auth/verify-reset-code → 코드 검증 (Redis 키 삭제)
          ↓
POST /api/auth/reset-password    → 비밀번호 변경
```

> ⚠️ 서버는 3단계(`reset-password`) 호출 시 2단계 완료 여부를 **검증합니다** — `reset_verified:{userId}` Redis 키(10분 TTL)가 없으면 400 오류 반환.

#### `POST /api/auth/send-reset-code` — 1단계: 인증코드 발송

Redis에 `reset_code:{user_id}` 키로 6자리 코드 저장 (TTL 3분), 등록된 이메일로 발송.

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `user_id` | string | ✅ | 사용자 ID |
| `mail` | string | ✅ | 등록된 이메일 (불일치 시 오류) |

**Response 200**
```json
{ "success": true, "message": "인증코드가 발송되었습니다.", "data": null }
```

**Response 404** (사용자 없음 또는 이메일 불일치)
```json
{ "success": false, "message": "사용자를 찾을 수 없습니다." }
```

---

#### `POST /api/auth/verify-reset-code` — 2단계: 인증코드 확인

코드 일치 시 Redis에서 `reset_code:{user_id}` 키 삭제.

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `user_id` | string | ✅ | 사용자 ID |
| `code` | string | ✅ | 이메일로 받은 6자리 코드 |

**Response 200**
```json
{ "success": true, "message": "인증코드가 확인되었습니다.", "data": null }
```

**Response 400** (코드 불일치 또는 만료)
```json
{ "success": false, "message": "유효하지 않은 인증코드입니다." }
```

---

#### `POST /api/auth/reset-password` — 3단계: 비밀번호 변경

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `user_id` | string | ✅ | 사용자 ID |
| `new_password` | string | ✅ | 새 비밀번호 (4자 이상) |

**Response 200**
```json
{ "success": true, "message": "비밀번호가 변경되었습니다", "data": null }
```

---

---

### `POST /api/auth/send-email-code` — 회원가입 이메일 인증코드 발송 ✨ 신규

Redis에 `email_verify:{mail}` 키로 6자리 코드 저장 (TTL 3분).

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `mail` | string | ✅ | 인증할 이메일 주소 |

**Response 200**
```json
{ "success": true, "message": "인증코드가 발송되었습니다.", "data": null }
```

---

### `POST /api/auth/verify-email-code` — 회원가입 이메일 인증코드 확인 ✨ 신규

코드 일치 시 Redis에서 `email_verify:{mail}` 키 삭제.

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `mail` | string | ✅ | 인증한 이메일 주소 |
| `code` | string | ✅ | 이메일로 받은 6자리 코드 |

**Response 200**
```json
{ "success": true, "message": "이메일 인증이 완료되었습니다.", "data": null }
```

**Response 400** (코드 불일치 또는 만료)
```json
{ "success": false, "message": "유효하지 않은 인증코드입니다." }
```

---

### `POST /api/auth/refresh` — 토큰 재발급 ✨ 신규

기존 Refresh Token을 검증해 새 Access Token + Refresh Token 발급.

**Header**

| 헤더 | 필수 | 설명 |
|------|------|------|
| `Refresh-Token` | ✅ | 발급받은 refresh token (Bearer 접두사 없음) |

**Response 200**
```json
{
  "success": true,
  "message": "요청이 성공했습니다.",
  "data": {
    "access_token": "eyJhbGc...",
    "refresh_token": "eyJhbGc...",
    "token_type": "Bearer"
  }
}
```

**Response 401** (만료 또는 유효하지 않은 토큰)
```json
{ "success": false, "message": "만료된 토큰입니다." }
```

---

## 👤 2. 유저 (User) — Kotlin 서버

> 모든 엔드포인트: `Authorization: Bearer {accessToken}` 필요  
> 토큰의 userId와 경로의 userId가 다를 경우 403 반환.

### `GET /api/users/{user_id}` — 유저 정보 조회

**Response 200**
```json
{
  "success": true,
  "data": {
    "user_id": "user001",
    "name": "홍길동",
    "mail": "hong@example.com",
    "phone": "010-1234-5678",
    "created_at": "2025-01-01T00:00:00"
  }
}
```

---

### `PUT /api/users/{user_id}` — 유저 정보 수정

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `name` | string | ❌ | 보호자 이름 |
| `mail` | string | ❌ | 이메일 |
| `phone` | string | ❌ | 전화번호 (형식: `010-0000-0000`) |
| `password` | string | ❌ | 새 비밀번호 (4자 이상) |

**Response 200**
```json
{ "success": true, "message": "개인정보가 수정되었습니다." }
```

---

### `PUT /api/users/{user_id}/fcm-token` — FCM 토큰 등록/갱신

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `fcm_token` | string | ✅ | FCM 토큰 값 |

**Response 200**
```json
{ "success": true, "message": "FCM 토큰 등록 완료", "data": null }
```

---

### `DELETE /api/users/{user_id}` — 회원 탈퇴

**Response 200**
```json
{ "success": true, "message": "회원 탈퇴가 완료되었습니다." }
```

---

### `POST /api/users/{user_id}/verify-password` — 비밀번호 사전 확인 ✨ 신규

> 개인정보 수정 화면 진입 전 본인 확인용. 비밀번호 불일치 시 401 반환.

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `current_password` | string | ✅ | 현재 비밀번호 |

**Response 200**
```json
{ "success": true, "message": "비밀번호가 확인되었습니다.", "data": null }
```

**Response 401** (비밀번호 불일치)
```json
{ "success": false, "message": "비밀번호가 올바르지 않습니다.", "data": null }
```

---

## 📷 3. 카메라 (Camera)

### ── Python AI 서버 ──

> 모든 Python AI 서버 엔드포인트: `Authorization: Bearer {accessToken}` 필요

### `WS /ws/stream` — landmark 스트리밍 + AI 추론 ⭐ 핵심 (v4.0 변경)

> **변경**: HTTP `POST /api/camera/stream` (JPEG multipart) → WebSocket `WS /ws/stream` (landmark JSON)  
> Android on-device MediaPipe가 landmark를 추출해 서버로 전송. 서버는 MediaPipe 미사용.

**연결**
```
ws://python-server:8000/ws/stream?token={accessToken}
```

**클라이언트 → 서버 메시지**

| type | 필드 | 설명 |
|------|------|------|
| `init` | `user_id`, `device_id` | 연결 초기화 (첫 메시지) |
| `frame` | `frame`: int, `timestamp`: float, `landmarks`: list[33] | 매 프레임 landmark 전송 |

```json
// init
{"type": "init", "user_id": "user001", "device_id": "device001"}

// frame
{
  "type": "frame",
  "frame": 42,
  "timestamp": 2.8,
  "landmarks": [{"x": 0.5, "y": 0.3, "z": -0.1, "v": 0.95}, ...]  // 33개
}
```

**서버 → 클라이언트 메시지**

```json
// init 응답
{"type": "init_ok"}

// 추론 결과 (WINDOW_SIZE=30 충족 + STRIDE=5 주기마다)
{
  "type": "result",
  "fall_score": 82.5,   // 0~100, 윈도우 미달 시 0.0
  "fall": true,
  "level": "위험",      // 정상 / 주의 / 위험
  "log_id": "a1b2c3d4-..."  // 이번 프레임에서 fall_log가 새로 저장된 경우에만 값 존재, 아니면 null
}
```

> `log_id`는 `score > CRITICAL_THRESHOLD`(위험) 또는 `fall=true`이거나 `score > WARNING_THRESHOLD`(주의)이고 각각의 쿨다운(6h/5min)을 통과해 실제로 `fall_log`가 저장된 프레임에서만 채워짐. 주의/위험 모두에서 채워질 수 있으나, 영상 업로드(`upload-url`)는 위험 등급에서만 허용(`VIDEO_NOT_ALLOWED`).

> AI 추론 내부 처리 흐름은 [부록 A](#부록-a-python-ai-서버-구현-상세) 참고.

**낙상 위험 레벨**

| 레벨 | score 범위 | v2.0 |
|------|-----------|------|
| 정상 | 0 ~ 50 | ~~0.0 ~ 0.4~~ |
| 주의 | 50 초과 ~ 75 | ~~0.4 ~ 0.7~~ |
| 위험 | 75 초과 | ~~0.7 ~ 1.0~~ |

> ⚠️ v4.2에서 재수정: v4.1은 경계값을 `>=`(51/76)로 통일했다고 기술했으나, 실제 코드(`RiskLevel.kt`, `app/ai/engine.py`)는 이후 커밋에서 다시 strict `>`(50/75)로 정정되어 현재까지 유지 중입니다.

---

### `GET /api/camera/score/{user_id}` — 현재 낙상 위험 점수 조회

**Response 200**
```json
{
  "user_id": "user001",
  "score": 82.5,        // 0~100
  "level": "위험"       // 정상 / 주의 / 위험
}
```

---

### `GET /api/camera/status/{device_id}` — 카메라 기기 상태 조회

**Response 200**
```json
{
  "device_id": "device01",
  "status": "active",         // active / inactive / offline
  "last_seen": "2026-05-09T12:00:00Z"
}
```

**Response 404**
```json
{ "detail": "기기를 찾을 수 없습니다" }
```

---

### `GET /api/camera/url/{device_id}` — 카메라 스트리밍 URL 조회

**Response 200**
```json
{
  "device_id": "device01",
  "camera_url": "rtsp://192.168.0.10:8554/stream"
}
```

**Response 404**
```json
{ "detail": "기기를 찾을 수 없습니다" }
```

---

### ── Kotlin 서버 ──

> 모든 Kotlin 서버 엔드포인트: `Authorization: Bearer {accessToken}` 필요

### `GET /api/camera/score/{user_id}` — 현재 낙상 위험 점수 조회 (Kotlin)

> Firestore `realtime_data/{userId}` 기반. Python AI 서버 응답과 달리 `updated_at` 포함.

**Response 200**
```json
{
  "success": true,
  "data": {
    "user_id": "user001",
    "score": 82.5,
    "level": "위험",
    "updated_at": "2026-06-03T14:00:00"
  }
}
```

---

### `PATCH /api/fall-logs/{userId}/{logId}/confirm` — 낙상 로그 확인 처리

> ⚠️ v3.0: Kotlin FallLogController 담당 (v2.0: Python 서버로 기술 + 경로 수정)

**Response 200**
```json
{
  "success": true,
  "data": {
    "logId": "abc123",
    "isConfirmed": true
  }
}
```

---

## 🚨 5. 낙상 로그 (Fall Logs) — Kotlin 서버

> 모든 엔드포인트: `Authorization: Bearer {accessToken}` 필요

### `GET /api/fall-logs/{user_id}` — 낙상 로그 목록 조회

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| `level` | string | ❌ | 레벨 필터: `위험` \| `주의` (생략 시 전체, 최대 100건) |

**Response 200**
```json
{
  "success": true,
  "data": {
    "logs": [
      {
        "log_id": "abc123",
        "device_id": "device01",
        "user_id": "user001",
        "score": 91.2,
        "fall": true,
        "is_confirmed": false,
        "has_video": true,
        "timestamp": "2026-05-09T11:00:00"
      }
    ]
  }
}
```

---

### `GET /api/fall-logs/{user_id}/counts` — 탭별 낙상 로그 건수 조회

**Response 200**
```json
{
  "success": true,
  "data": {
    "전체": 15,
    "위험": 8,
    "주의": 7
  }
}
```

---

### `GET /api/fall-logs/{user_id}/{log_id}` — 낙상 로그 단건 조회

**Response 200**
```json
{
  "success": true,
  "data": {
    "log_id": "abc123",
    "device_id": "device01",
    "user_id": "user001",
    "score": 91.2,
    "fall": true,
    "is_confirmed": false,
    "has_video": true,
    "timestamp": "2026-05-09T11:00:00"
  }
}
```

---

### `GET /api/fall-logs/{user_id}/{log_id}/video` — 동영상 signed URL 조회 (v4.2, ~~/thumbnail~~ 대체)

> 1시간 유효한 GCS V4 Signed URL을 반환합니다. 동영상이 없으면 404.  
> ~~`/download` 엔드포인트는 삭제됨 (v4.1)~~ ~~`/thumbnail`~~ 은 v4.2에서 MP4 전환에 맞춰 `/video`로 개명됨.

**Response 200**
```json
{ "success": true, "data": { "signed_url": "https://storage.googleapis.com/..." } }
```

**Response 404** (동영상 없음)
```json
{ "success": false, "message": "동영상이 존재하지 않습니다.", "data": null }
```

---

### `POST /api/fall-logs/{user_id}/{log_id}/upload-url` — 동영상 업로드용 signed URL 발급 ✨ v4.2 신규

> Android가 GCS에 낙상 mp4 클립을 직접 업로드할 수 있도록 10분 유효한 signed PUT URL을 발급합니다.  
> **위험(75 초과) 등급 로그에만 허용** — 주의 등급 로그는 `VIDEO_NOT_ALLOWED`(403) 반환. 업로드 요청은 `Content-Type: video/mp4` 헤더 필수.

**Response 200**
```json
{
  "success": true,
  "data": {
    "upload_url": "https://storage.googleapis.com/...(PUT, TTL 10분)",
    "content_type": "video/mp4"
  }
}
```

**Response 403** (주의 등급)
```json
{ "success": false, "message": "주의 등급 이벤트는 동영상을 제공하지 않습니다.", "data": null }
```

> 업로드 시퀀스: `POST upload-url` → 반환된 URL로 `PUT`(Android→GCS 직접) → `PATCH video-complete`

---

### `PATCH /api/fall-logs/{user_id}/{log_id}/video-complete` — 동영상 업로드 완료 콜백 ✨ v4.2 신규

> Android가 signed PUT URL로 GCS 업로드를 마친 뒤 호출합니다. 클라이언트의 성공 주장만으로 `video_url`을 반영하지 않고,
> 서버가 GCS에 실제 객체가 존재하는지 재확인한 뒤에만 Firestore를 갱신합니다. 위험 등급 전용 정책은 이 엔드포인트에도 동일 적용.  
> ⚠️ 콜백이 유실된 경우(네트워크 단절 등)를 대비해 `FallLogVideoReconciliationJob`이 15분 주기로 누락분을 자동 보정합니다 — 이 엔드포인트 호출 실패가 곧 영구 데이터 유실을 의미하지는 않습니다.

**Response 200**
```json
{
  "success": true,
  "message": "동영상 업로드가 반영되었습니다.",
  "data": {
    "log_id": "abc123",
    "has_video": true
  }
}
```

**Response 404** (GCS에 객체가 아직 없음 — 업로드 미완료 또는 실패)
```json
{ "success": false, "message": "동영상이 존재하지 않습니다.", "data": null }
```

---

### `DELETE /api/fall-logs/{user_id}/{log_id}` — 낙상 로그 삭제

**Response 200**
```json
{ "success": true, "message": "낙상 로그가 삭제되었습니다." }
```

---

## ⚙️ 6. 설정 (Settings) — Kotlin 서버

> 모든 엔드포인트: `Authorization: Bearer {accessToken}` 필요

### `GET /api/settings/notifications/{user_id}` — 알림 설정 조회

**Response 200**
```json
{
  "success": true,
  "data": {
    "notification_enabled": true,
    "sound_enabled": true,
    "vibration_enabled": true
  }
}
```

---

### `PUT /api/settings/notifications/{user_id}` — 알림 설정 변경

**Request Body** (모든 필드 선택 사항)

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `notification_enabled` | boolean | ❌ | 알림 활성화 여부 |
| `sound_enabled` | boolean | ❌ | 소리 알림 여부 |
| `vibration_enabled` | boolean | ❌ | 진동 알림 여부 |

**Response 200**
```json
{
  "success": true,
  "message": "알림 설정 변경 완료",
  "data": {
    "notification_enabled": true,
    "sound_enabled": false,
    "vibration_enabled": true
  }
}
```

---

### `GET /api/settings/retention/{user_id}` — 로그 보관 기간 조회

> 보관 기간은 서버 고정 30일입니다. PUT(변경) 엔드포인트는 제공하지 않습니다.

**Response 200**
```json
{ "success": true, "data": { "retention_days": 30 } }
```

---

### `GET /api/settings/marketing/{user_id}` — 마케팅 수신 동의 조회 ✨ v4.2 신규

**Response 200**
```json
{
  "success": true,
  "data": {
    "consent": false,
    "consented_at": null
  }
}
```

---

### `PUT /api/settings/marketing/{user_id}` — 마케팅 수신 동의 변경 ✨ v4.2 신규

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `consent` | boolean | ✅ | 마케팅 수신 동의 여부 |

**Response 200**
```json
{
  "success": true,
  "message": "마케팅 수신 동의 변경 완료",
  "data": {
    "consent": true,
    "consented_at": "2026-08-05T10:00:00"
  }
}
```

---

## 📱 7. 기기 (Devices) — Python AI 서버

> 모든 엔드포인트: `Authorization: Bearer {accessToken}` 필요

### `GET /api/devices/{user_id}` — 등록 기기 목록 조회

**Response 200**
```json
{
  "devices": [
    {
      "device_id": "a1b2c3d4e5f6...",
      "device_name": "Samsung Galaxy S24",
      "status": "active",
      "last_seen": "2026-05-09T12:00:00Z"
    }
  ]
}
```

---

### `POST /api/devices/{user_id}` — 기기 등록

> Android 로그인 성공 직후 자동 호출. `device_id`는 `ANDROID_ID` 사용.

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `device_id` | string | ✅ | 기기 고유 ID (`ANDROID_ID`) |
| `device_name` | string | ✅ | 기기 이름 (`Build.MODEL`) |
| `camera_url` | string | ❌ | 카메라 스트리밍 URL |

**Response 201** (신규 등록)
```json
{ "status": "created", "device_id": "a1b2c3d4e5f6..." }
```

**Response 200** (다른 사용자 소유 기기 재귀속)
```json
{ "status": "updated", "device_id": "a1b2c3d4e5f6..." }
```

**Response 409** (동일 사용자 재등록)
```json
{ "detail": "이미 등록된 기기 ID입니다" }
```

> 기기가 이전에 다른 user_id로 등록된 경우 현재 사용자로 소유권이 이전됩니다 (재로그인·기기 이전 대응).

---

## 🔧 8. 내부 API (Internal) — Kotlin 서버 ✨ 신규

> **`POST /api/notification/send` 는 v4.1에서 삭제됨.** FCM 알림은 `/internal/fall-log` 경유로만 발송되며 외부 직접 호출 엔드포인트는 제공하지 않습니다.

> **JWT 인증 없음 — 로컬 네트워크 전용 (Python AI 서버 → Kotlin 서버)**

### `POST /internal/realtime` — 추론 결과 realtime_data 저장

> Python AI 서버가 매 추론 후 호출 → `realtime_data/{userId}` 고정 문서 갱신 → `GET /api/camera/score` 정상화

**Request Body**

> ⚠️ Kotlin 전역 SNAKE_CASE 정책 적용 — 필드명 반드시 snake_case로 전송

| 필드 | 타입 | 설명 |
|------|------|------|
| `user_id` | string | 사용자 ID |
| `score` | float | 위험 점수 (0~100) |
| `level` | string | 위험 레벨 (`정상` / `주의` / `위험`) |

**Response 200**
```json
{ "success": true, "message": "요청이 성공했습니다." }
```

---

### `POST /internal/fall-log` — 낙상 로그 저장 + FCM 발송

> `score > 75(위험) OR fall == true` 시 위험 6시간 쿨다운(`danger_cd`) 통과, 또는 `score > 50(주의)`이고 주의 5분 쿨다운(`caution_cd`) 통과 시 Python이 호출 → Kotlin이 `fall_logs` Firestore 저장 + FCM 푸시 발송  
> ⚠️ FCM 실패 시에도 DB 저장은 완료 보장 (`sendNotificationSafe()` 내부 예외 흡수). AI 서버 재시도로 인한 중복 `fall_log` 방지.

**Request Body**

> ⚠️ Kotlin 전역 SNAKE_CASE 정책 적용 — 필드명 반드시 snake_case로 전송

| 필드 | 타입 | 설명 |
|------|------|------|
| `log_id` | string | UUID (Python이 생성해 전달) |
| `device_id` | string | 기기 ID |
| `user_id` | string | 사용자 ID |
| `score` | float | 위험 점수 (0~100) |
| `fall` | boolean | 낙상 감지 여부 |
| `is_confirmed` | boolean | 기본 `false` |

**Response 200**
```json
{ "success": true, "message": "요청이 성공했습니다." }
```

---

## 📊 전체 엔드포인트 요약 (38 REST + 1 WS) — v4.2 기준 재실측

> 죽은 카메라 라이브 릴레이(`/ws/camera`, 카메라 세션 3종, `PUT /api/camera/url`(Kotlin), `POST /internal/frame`) 제거 — 카메라 장치용 producer가 존재한 적이 없는 완전한 죽은 코드였음을 확인 후 삭제 (PR #23).

### Python AI 서버 (5 REST + 1 WS)

| # | 메서드 | URL | 설명 | 인증 |
|---|--------|-----|------|------|
| 1 | `WS` | `/ws/stream` | landmark 스트리밍 + AI 추론 (v4.0 변경) | `?token=` |
| 2 | `GET` | `/api/camera/score/{user_id}` | 위험 점수 조회 (Redis 캐시) | ✅ JWT |
| 3 | `GET` | `/api/camera/status/{device_id}` | 기기 상태 조회 (active/inactive/offline) | ✅ JWT |
| 4 | `GET` | `/api/camera/url/{device_id}` | 스트리밍 URL 조회 | ✅ JWT |
| 5 | `GET` | `/api/devices/{user_id}` | 기기 목록 조회 | ✅ JWT |
| 6 | `POST` | `/api/devices/{user_id}` | 기기 등록 | ✅ JWT |

### Kotlin Spring 서버 (33개 REST)

| # | 메서드 | URL | 설명 | 인증 |
|---|--------|-----|------|------|
| 7 | `POST` | `/api/auth/login` | 로그인 (JWT 발급, 로그인 이력 저장 ✨ v4.2) | - |
| 8 | `POST` | `/api/auth/logout` | 로그아웃 (토큰 블랙리스트) | - |
| 9 | `POST` | `/api/auth/register` | 회원가입 | - |
| 10 | `POST` | `/api/auth/check-id` | 아이디 중복확인 | - |
| 11 | `POST` | `/api/auth/find-id` | 아이디 찾기 | - |
| 12 | `POST` | `/api/auth/send-email-code` | 회원가입 이메일 인증코드 발송 | - |
| 13 | `POST` | `/api/auth/verify-email-code` | 회원가입 이메일 인증코드 확인 | - |
| 14 | `POST` | `/api/auth/send-reset-code` | 비밀번호 재설정 1단계 (코드 발송) | - |
| 15 | `POST` | `/api/auth/verify-reset-code` | 비밀번호 재설정 2단계 (코드 검증) | - |
| 16 | `POST` | `/api/auth/reset-password` | 비밀번호 재설정 3단계 (변경) | - |
| 17 | `POST` | `/api/auth/refresh` | 토큰 재발급 | - |
| 18 | `GET` | `/api/users/{user_id}` | 유저 정보 조회 | ✅ JWT |
| 19 | `PUT` | `/api/users/{user_id}` | 유저 정보 수정 | ✅ JWT |
| 20 | `POST` | `/api/users/{user_id}/verify-password` | 비밀번호 사전 확인 ✨ v4.1 | ✅ JWT |
| 21 | `PUT` | `/api/users/{user_id}/fcm-token` | FCM 토큰 등록/갱신 | ✅ JWT |
| 22 | `DELETE` | `/api/users/{user_id}` | 회원 탈퇴 (cascade 삭제 ✨ v4.2) | ✅ JWT |
| 23 | `GET` | `/api/camera/score/{user_id}` | 위험 점수 + updated_at 조회 (Firestore) | ✅ JWT |
| 24 | `GET` | `/api/camera/status/{user_id}` | 위험 레벨 조회 (정상/주의/위험) | ✅ JWT |
| 25 | `GET` | `/api/fall-logs/{user_id}` | 낙상 로그 목록 | ✅ JWT |
| 26 | `GET` | `/api/fall-logs/{user_id}/counts` | 탭별 로그 건수 (전체/위험/주의) | ✅ JWT |
| 27 | `GET` | `/api/fall-logs/{user_id}/{log_id}` | 낙상 로그 단건 | ✅ JWT |
| 28 | `GET` | `/api/fall-logs/{user_id}/{log_id}/video` | 동영상 signed URL 조회 ✨ v4.2 (~~/thumbnail~~) | ✅ JWT |
| 29 | `POST` | `/api/fall-logs/{user_id}/{log_id}/upload-url` | 동영상 업로드용 signed URL 발급 ✨ v4.2 신규 | ✅ JWT |
| 30 | `PATCH` | `/api/fall-logs/{user_id}/{log_id}/video-complete` | 동영상 업로드 완료 콜백 ✨ v4.2 신규 | ✅ JWT |
| 31 | `PATCH` | `/api/fall-logs/{user_id}/{log_id}/confirm` | 낙상 확인 처리 | ✅ JWT |
| 32 | `DELETE` | `/api/fall-logs/{user_id}/{log_id}` | 낙상 로그 삭제 | ✅ JWT |
| 33 | `GET` | `/api/settings/notifications/{user_id}` | 알림 설정 조회 | ✅ JWT |
| 34 | `PUT` | `/api/settings/notifications/{user_id}` | 알림 설정 변경 | ✅ JWT |
| 35 | `GET` | `/api/settings/retention/{user_id}` | 보관 기간 조회 (고정 30일) | ✅ JWT |
| 36 | `GET` | `/api/settings/marketing/{user_id}` | 마케팅 수신 동의 조회 ✨ v4.2 신규 | ✅ JWT |
| 37 | `PUT` | `/api/settings/marketing/{user_id}` | 마케팅 수신 동의 변경 ✨ v4.2 신규 | ✅ JWT |
| 38 | `POST` | `/internal/realtime` | 추론 결과 realtime_data 저장 (내부용) | ❌ |
| 39 | `POST` | `/internal/fall-log` | 낙상 로그 저장 + FCM 발송 (내부용) | ❌ |

> ✨ v4.2 신규  |  총 REST **38개** (Kotlin 33 + Python 5) + WS 1개  
> v4.2 변경: `/thumbnail`→`/video` 개명, `upload-url`/`video-complete`/마케팅 동의 GET·PUT 4개 신규, 죽은 카메라 릴레이 관련 엔드포인트(구 v4.1까지 카운트에 포함되지 않았던 항목 포함) 완전 제거, Python 섹션을 5 REST + 1 WS로 명확히 분리 표기(이전까지 WS가 "6개 REST"로 오표기됨)  
> Kotlin `GET /api/camera/score`는 Firestore realtime_data 기반(`updated_at` 포함), Python `GET /api/camera/score`는 Redis 캐시 기반 (포트 상이: 8080 vs 8000)

---

## 🔬 부록 A. Python AI 서버 구현 상세

> 앱 개발에는 불필요한 서버 내부 구현 정보입니다.  
> 파일 위치: `app/domain/camera/service.py`, `app/ai/engine.py`, `app/ai/buffer.py`

---

### A-1. `WS /ws/stream` 처리 흐름 (v4.0 변경)

```
[Android on-device]
  MediaPipe Pose → landmark 33개 추출 → WebSocket 전송

[Python AI 서버]
1.  init 메시지 수신 → user_id, device_id 바인딩
2.  frame 메시지 수신 → device_id별 deque 버퍼에 누적
3.  버퍼 < WINDOW_SIZE(30)이면 {"type":"result","fall_score":0.0} 반환 (버퍼 미달)
4.  STRIDE(5) 주기마다 Step2~6 전처리 + XGBoost 추론 실행
5.  Savitzky-Golay 필터로 좌표 노이즈 제거 (window=7, poly=2)
6.  골반 중앙정렬 → 골반 간 거리 정규화
7.  15개 관절 × 3 (각도·각속도·각가속도) + center 2개 = 47개 피처 계산
8.  무게중심 동역학 6개 피처 계산 (speed, acceleration, displacement 등)
9.  XGBoost 모델 + StandardScaler → score(0~100), fall(bool) 추론. `_classify_level(score)`로 level(정상/주의/위험) 분류
10. POST /internal/realtime → Kotlin이 realtime_data/{userId} 단일 문서 갱신
    (저장 필드: score, level, updated_at — Python의 Firestore 직접 쓰기 없음)
11. score > 75(위험) OR fall == True → danger_cd(6h) 쿨다운 통과 시 POST /internal/fall-log → Kotlin이 fall_logs 저장 + FCM 발송
    (score 50 초과~75(주의): caution_cd(5min) 쿨다운 통과 시에만 fall-log 호출)
    (9단계 score는 15분 sticky floor(apply_score_floor) 적용값 — 위험 진입 후 15분간 하락 방지, 상승은 허용)
```

---

### A-2. Redis 내부 키 상세

| 키 | 용도 | TTL | 비고 |
|----|------|-----|------|
| `score:{user_id}` | 최신 추론 결과 캐시 `{"score": float, "level": string}` | 30초 | Python `GET /api/camera/score` 에서 읽음 |
| `caution_cd:{user_id}` | 주의(50 초과~75) 이벤트 중복 알림 방지 쿨다운 플래그 | 5분 | NX 플래그로 5분 내 재발송 방지 |
| `danger_cd:{user_id}` | 위험(75 초과)·`fall` 이벤트 중복 알림 방지 쿨다운 플래그 | 6시간 | NX 플래그로 6시간 내 재발송 방지 |
| `score_floor:{user_id}` | 위험 진입 시 최고 score 유지 (하락 방지, 상승은 허용) | 15분 | `apply_score_floor()` — 지속 위험 시 TTL 매번 연장 |

> ⚠️ v4.0 이전에 사용하던 `frame_count:{device_id}` (추론 카운터), `frame:{device_id}` (JPEG bytes) 키는 `buffer.py` 리팩토링(`push_frame_count()`, `should_infer()`, `save_latest_frame()` 제거)으로 더 이상 사용되지 않습니다. 추론 윈도우 버퍼는 서버 메모리 내 deque로 관리합니다.

---

### A-3. `realtime_data` Firestore 컬렉션

> **문서 ID = `userId` (사용자당 1개, 추론마다 덮어쓰기).**  
> Python이 `POST /internal/realtime` 호출 → Kotlin `InternalService`가 저장.  
> 클라이언트 직접 조회 API 없음 — `GET /api/camera/score`, `GET /api/camera/status`를 통해서만 접근.

| 필드 | 타입 | 설명 |
|------|------|------|
| `score` | float | 최신 낙상 위험 점수 (0~100) |
| `level` | string | 위험 레벨 (`정상` / `주의` / `위험`) |
| `updated_at` | timestamp | 마지막 갱신 시각 |

> ⚠️ **미구현 — ML 분석용 51개 피처 auto-ID 문서 저장**  
> 초기 설계에서는 관절 피처 51개를 매 추론마다 별도 문서(auto-ID)로 쌓아 모델 재학습 데이터셋으로 활용하는 구조를 고려했으나, 현재는 구현되지 않았습니다.  
> 필요 시 `ASIS-TOBE` 스펙 문서를 별도 작성 후 구현합니다.

---

### A-4. AI 모델 정보

| 항목 | 값 |
|------|-----|
| 포즈 추출 | Android on-device MediaPipe Pose → landmark JSON 전송 (서버 측 MediaPipe 없음) |
| 분류 모델 | **XGBoost** (`pkl/xgb_model.pkl`) — Decision Tree에서 교체 (v4.0) |
| 스케일러 | StandardScaler (`pkl/scaler.pkl`) |
| 노이즈 필터 | Savitzky-Golay (window=7, poly=2) |
| 추론 방식 | `predict_proba()[1] × 100` → score, `predict()` → fall, `_classify_level()` → level |
| 비동기 처리 | `loop.run_in_executor()` (스레드 풀) |
| 윈도우 버퍼 | 서버 메모리 내 deque (WINDOW_SIZE=30, STRIDE=5) |
